services:
  scan-collector:
    build: .
    container_name: scan-collector
    restart: unless-stopped
    userns_mode: "keep-id:uid=1000,gid=1000"
    ports:
      - "8025:8025"
    volumes:
      - ./scans:/scans:Z
      - ./certs:/certs:Z,ro
    security_opt:
      - no-new-privileges:true
